<?php
use  Drupal\Core\Entity\EntityInterface;


/**
 * Prevent publishing of template type regardless of permissions
*/
function county_job_openings_entity_presave(EntityInterface $entity) {
  if ($entity->bundle() == 'template_jobs') {
    $entity->setUnpublished();
  }
}
